import { FormatNameTitlePipe } from './format-name-title.pipe';

describe('FormatNameTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatNameTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
